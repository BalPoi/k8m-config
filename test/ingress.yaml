apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bal-ingress
spec:
  rules:
    - http:
        paths:
          - path: /bal-server
            pathType: Prefix
            backend:
              service:
                name: bal-server
                port:
                  number: 8080
